import 'package:flutter/material.dart';

class SmallScreenDevices extends StatelessWidget {
  const SmallScreenDevices({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Small Screen Devices',
      ),
    );
  }
}
