import 'package:flutter/material.dart';

class CustomScreenDevices extends StatelessWidget {
  const CustomScreenDevices({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Custom Screen Devices',
      ),
    );
  }
}
